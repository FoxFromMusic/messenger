📱 Messenger API

Тестовое задание на разработку API для управления чатами и сообщениями.

🛠 Стек технологий

    Python 3.10+

    FastAPI — веб-фреймворк

    PostgreSQL — база данных

    SQLAlchemy — ORM

    Alembic — миграции базы данных

    Docker & Docker Compose — контейнеризация

🚀 Запуск проекта

    Клонируйте репозиторий:

    git clone <ссылка_на_ваш_репозиторий>
    cd Messanger

    Запустите контейнеры:

    docker-compose up -d --build

    Примените миграции (создание таблиц в БД): (Важный шаг, так как таблицы создаются через Alembic)
    Bash

    docker-compose exec web alembic upgrade head

    Откройте документацию: Перейдите по адресу http://localhost:8000/docs для доступа к Swagger UI.

📂 Структура API

    POST /chats/ — создание нового чата.

    DELETE /chats/{chat_id} — удаление чата и всех его сообщений (cascade).

    POST /chats/{chat_id}/messages/ — отправка сообщения в чат.

    GET /chats/{chat_id}/messages/ — получение последних N сообщений чата.

📝 Особенности реализации

    Миграции: Схема базы данных полностью управляется Alembic.

    Валидация: Текст сообщений проверяется на пустоту и максимальную длину (Pydantic).

    Каскадное удаление: При удалении чата база данных автоматически очищает все связанные с ним сообщения.

    Docker: Проект полностью изолирован и готов к развертыванию в любом окружении
